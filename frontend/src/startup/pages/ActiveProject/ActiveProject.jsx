import React, { useState } from 'react'
import Sidebar from '@/startup/components/Sidebar'
import Layout from '@/startup/Layout/Layout';
const ActiveProject = () => {
    const [sidebarOpen, setSidebarOpen] = useState(false);

  return (
    <Layout>

    <div>lo</div>
    </Layout>
  )
}

export default ActiveProject